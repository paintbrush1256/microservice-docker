On AWS
======
sudo snap install microk8s --classic
sudo snap alias microk8s.kubectl kubectl
sudo usermod -a -G microk8s ubuntu
microk8s.start

sudo microk8s status
sudo microk8s enable registry
sudo microk8s.enable registry:size=2Gi

sudo microk8s disable registry
sudo microk8s disable storage:destroy-storage


Note :- Error in pulling images by kubernetes


#sudo microk8s inspect
#sudo microk8s.kubectl config view

#sudo chown -f -R ubuntu ~/.kube


#kubectl config set-context microk8s
#microk8s.kubectl config view --raw > $HOME/.kube/config

